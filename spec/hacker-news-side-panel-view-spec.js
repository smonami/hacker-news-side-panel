'use babel';

import HackerNewsSidePanelView from '../lib/hacker-news-side-panel-view';

describe('HackerNewsSidePanelView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
